# Chat_Application
It is an android chat application. I created this application using android (java). In these application, I used firebase realtime database for user authentication and for storing message data.
